@model List<lab_2.Models.OrderViewModel>

<h2>Orders</h2>

<p>
    <a class="btn" asp-controller="Order" asp-action="Create">
        Create New Order
    </a>
</p>

<table class="orders-table">
    <tr>
        <th>Id</th>
        <th>Event Type</th>
        <th>Date</th>
        <th>Price</th>
        <th>Actions</th>
    </tr>

    @for (int i = 0; i < Model.Count; i++)
    {
        <tr>
            <td>@Model[i].Id</td>
            <td>@Model[i].EventType</td>
            <td>@Model[i].EventDate.ToShortDateString()</td>
            <td>@Model[i].Price</td>
            <td>
                <a asp-controller="Order" asp-action="Details" asp-route-id="@Model[i].Id">
                    View
                </a>

                |

                <a asp-controller="Order" asp-action="Edit" asp-route-id="@Model[i].Id">
                    Edit
                </a>
            </td>
        </tr>
    }
</table>
